# Windows docker container


To get it working, just run ```bash setup.sh``` and introduce the requested variables!

Bear in mind:
- You should pick an unused port, otherwise you won't be able to access the container.
- You won't need to use the RDP port, but it's important to choose one that is free, otherwise the Docker image might crash